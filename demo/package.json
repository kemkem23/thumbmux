{
  "name": "thumbmux-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "start": "bun run build && bun serve.ts",
    "demo": "bun run start"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "svelte": "^5.51.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "vite": "^7.3.1"
  }
}
