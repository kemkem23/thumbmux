import { type AppAdapters } from './config';
type $$ComponentProps = {
    session: string;
    adapters: AppAdapters;
};
declare const SessionView: import("svelte").Component<$$ComponentProps, {}, "">;
type SessionView = ReturnType<typeof SessionView>;
export default SessionView;
